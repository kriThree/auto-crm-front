# Используем Node.js как основу
FROM node:20

# Устанавливаем рабочую директорию
WORKDIR /app

# Копируем пакетный менедж
COPY package*.json ./

# Устанавливаем зависимости
RUN npm i

# Копируем остальные файлы
COPY . .

# Открываем порт, на который слушает Vite приложение
EXPOSE 5173

# Устанавливаем команду запуска
CMD ["npm", "run", "dev"]